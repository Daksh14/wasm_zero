[package]
name = "wasm_zero_serve"
version.workspace = true
edition.workspace = true

[[bin]]
name = "serve"
path = "src/main.rs"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }
tower-http = { version = "0.6", features = ["fs"] }
